$(document).ready(function(){
console.log('create');
<% if @order.errors.any? || @order_item.errors.any? %>
alert("not valid.");
<% else %>
  $(".cart-text").html("<%= escape_javascript(render(:partial =>'shared/cart_text')).html_safe %>");
<% end %>
});
