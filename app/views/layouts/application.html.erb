<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>AppointmentProject03</title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= stylesheet_link_tag "http://yandex.st/bootstrap/3.0.2/css/bootstrap.min.css", media: "all" %>
    <%= javascript_include_tag "http://yandex.st/bootstrap/3.0.2/js/bootstrap.min.js" %>
    <%= csrf_meta_tags %>
</head>
<body>
    <nav class="navbar navbar-inverse navbar-fixed-top">
       <div class="container">
         <div class="navbar-header">
           <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
             <span class="sr-only">Toggle navigation</span>
             <span class="icon-bar"></span>
             <span class="icon-bar"></span>
             <span class="icon-bar"></span>
           </button>
          <div class="navbar-brand"><%= link_to "Schedule", "/" %></div>
         </div>
         <div id="navbar" class="navbar-collapse collapse">
           <% if current_user %>
           <ul class="nav navbar-nav">
             <li><%= link_to "Appointments", user_appointment_path(current_user) %></li>
             <li><%= link_to "Services", service_path(current_user) %></li>
             <li><%= link_to "Log Out", logout_path %></li>
             <li class="active-user">Logged in as: <strong><%= current_user.username %></strong></li>
           </ul>
             <% else %>
             <%= simple_form_for :user, url: "/sessions", method: "post", :html => {:class => "navbar-form navbar-right"} do |f| %>
               <div class="form-group">
                 <%= f.text_field :email, placeholder: "Email", class: "form-control" %>
               </div>
               <div class="form-group">
                 <%= f.password_field :login, placeholder: "Password", class: "form-control" %>
               </div>
               <div class="form-group">
               <%= f.submit "Sign In", class: "btn btn-success" %>
               </div>
             <% end %>
           <% end %>
         </div><!--/.navbar-collapse -->
       </div>
     </nav>
     <% flash.each do |type, message| %>
  <div class="alert <%= alert_class_for(type) %> alert-dismissible fade in">
    <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
    <%= message %>
  </div>
<% end %>
     <%= yield %>
   </body>
</html>
