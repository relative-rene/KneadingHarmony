<%= form_for([@user, @user.appointments.build]) do |f| %>
  <p>
    <%= f.label :service %><br>
    <%= f.text_field :service %>
  </p>
  <p>
    <%= f.label :date %><br>
    <%= f.text_area :date %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
