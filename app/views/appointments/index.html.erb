<div class="container">
  <div class="row">
    <h1>Welcome to the Appointment page!</h1>
    <hr>
  </div>
  <div class="row">
    <% @appointment.each do |t|%>
      <div class="row appointment">
        <div class="col-md-6">
          <h3><%=link_to "#{t.user_id}", new_appointment_path(t)%></h3>
          <p><%= "#{t.time}" %></p>
          <p>email: <%= "#{t.user.email}" %></p>
        </div>
      </div>
    <%end%>
  </div>
</div>
